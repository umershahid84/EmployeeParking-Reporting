{
  "name": "employee-parking-reporting",
  "version": "1.0.0",
  "private": true,
  "description": "Employee Parking Daily Reporting Application (single deployable app: Express API + built React client)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspace=client",
    "start": "npm run start --workspace=server",
    "dev": "concurrently -k -n SERVER,CLIENT -c blue,green \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "migrate": "npm run migrate --workspace=server",
    "seed:admin": "npm run seed:admin --workspace=server",
    "seed:reports": "npm run seed:reports --workspace=server --"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
